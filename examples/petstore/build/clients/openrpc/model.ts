// Generated by wiz from Petstore RPC.
// Types the API exchanges. Edit the document, not this file.

export interface PetRpcApi {
  /** Lists pets, filtered and capped. */
  listPets: listPets;
  /** Fetches one pet by id. */
  getPet: getPet;
  /** Adds a pet to the store. */
  addPet: addPet;
  /** Sells a pet to an owner. */
  sellPet: sellPet;
}

export type listPets = Record<string, unknown>;

export type getPet = Record<string, unknown>;

export type addPet = Record<string, unknown>;

export type sellPet = Record<string, unknown>;

export interface PetQuery {
  /** Restrict to one status. */
  status?: PetStatus;
  /** Substring match on the name, case-insensitive. */
  q?: string;
  limit?: number;
}

export type PetStatus = number;

export interface Pet {
  id: number;
  /** The pet's call name. */
  name: string;
  species: Species;
  status: PetStatus;
  /** Retail price in minor units, so no float ever touches money. */
  priceCents: string;
  tags: string[];
  /** Absent while the pet is still available. */
  owner?: Owner;
  addedAt: string;
}

export type Species = number;

export interface Owner {
  id: string;
  name: string;
  email: string;
}

export interface NewPet {
  name: string;
  species: Species;
  priceCents: string;
  tags?: string[];
}

export interface Sale {
  ownerId: string;
  ownerName: string;
  ownerEmail: string;
}
