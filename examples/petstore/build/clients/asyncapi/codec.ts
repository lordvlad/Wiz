// Generated by wiz from Petstore Events.
// Wire encoders and decoders. Edit the document, not this file.

import type { PetChanged } from "./model.ts";

export function encodePetChanged(val: PetChanged): string {
  return JSON.stringify(val);
}

export function decodePetChanged(raw: string): PetChanged {
  return typeof raw === "string" ? JSON.parse(raw) : raw;
}
